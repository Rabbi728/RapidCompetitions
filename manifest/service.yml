apiVersion: v1
kind: Service
metadata:
  name: ostadassignmant-service
  namespace: production
spec:
  type: NodePort
  selector:
    app: ostadassignmant
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 8000
